package com.espezzialy.simpleplayer.presentation.common

import androidx.compose.ui.graphics.Color

/** Shared dark UI colors (Songs, album, player). */
object SimplePlayerDarkPalette {
    val Background = Color(0xFF000000)
    val OnBackground = Color(0xFFFFFFFF)
    val OnBackgroundMuted = Color(0xFFB3B3B3)
    val Surface = Color(0xFF1C1C1E)
    val ControlSurface = Color(0xFF2C2C2C)
    val ArtworkPlaceholder = Color(0xFF1A1A1A)
}
